# Optional public API base for the browser app.
# Local development can usually leave this unset and use the built-in Vite /api proxy.
# Split frontend/backend deployments should point this at the public backend URL.
VITE_OPENAGENTGRAPH_API_BASE_URL=https://api.example.com
