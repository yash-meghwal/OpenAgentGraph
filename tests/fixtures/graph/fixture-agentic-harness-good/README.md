# Agentic Harness Good Fixture

## Setup

Run `npm ci` to install dependencies on a fresh clone.

## Test

Run `npm test` for unit tests and `npm run lint` before opening a PR.

## Build

Run `npm run build` for a production compile check.